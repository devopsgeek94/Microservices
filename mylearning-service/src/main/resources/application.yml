# Spring properties
spring:
  application:
     name: mylearning-service
  data:
    mongodb:
      database: mylearning
      host: 127.0.0.1
      port: 27017

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 2222   # HTTP (Tomcat) port